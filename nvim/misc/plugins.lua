----------------------------------------------------------------------------
-- Plugins Management
----------------------------------------------------------------------------

require("lazy").setup()

-- require('packer').startup(function()
--   -- Packer can and should manage itself
--   use 'wbthomason/packer.nvim'
--   -- Plenetary compilation of useful lua functions
--   use 'nvim-lua/plenary.nvim'
--   -- Plugins which don't need a config
--   use 'tpope/vim-commentary'
-- end)

-- Load multiples plugins and there config files
-- the loading is done within the config files
-- load('plugins.colorthemes')
-- load('plugins.mason')
-- load('plugins.coc')
-- load('plugins.lsp')
-- load('plugins.dap')
-- load('plugins.notify')
-- load('plugins.telescope')
-- load('lightline')
-- load('~/.config/nvim/plugins/netrw.vim')
