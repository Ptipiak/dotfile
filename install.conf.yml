- link:
      ~/.config/nvim: 
            path: ./nvim
            create: true
      ~/.profile:
            path: ./utils/bash/.profile
            create: true
      ~/.config/utils/auto-agent.sh:
          create: ~/.config/utils
          path: ./utils/bash/auto-agent.sh
          create: true

- shell:
    - ["nvim +PlugUpdate +PlugClean! +qa", Installing and updating plugins]
